theme: jekyll-theme-cayman
title: "PhoneStream — an old Android phone as a fast, always-on drive for macOS"
description: "Free & open-source. Automatic USB↔Wi-Fi failover, no-copy video streaming to IINA, and a phone-side server that actually stays alive (unlike tap-to-start FTP file-manager apps). Browse, transfer and stream from the Mac menubar."
show_downloads: true
